int str_len(char *s); // 3 points
int all_letters(char *s); // 5 points
int num_in_range(char *s1, char b, char t); // 5 points

int diff(char *s1, char *s2); // 8 points

void shorten(char *s, int new_len); // 5 points
int len_diff(char *s1, char *s2); // 5 points
void rm_left_space(char *s); // 5 points
void rm_right_space(char *s); // 5 points
void rm_space(char *s); // 5 points
int find(char *h, char *n); // 10 points
char *ptr_to(char *h, char *n); // 10 points
int is_empty(char *s); // 8 points
char *str_zip(char *s1, char *s2); // 10 points
void capitalize(char *s); // 10 points

int strcmp_ign_case(char *s1, char *s2); // 8 points

void take_last(char *s, int n); // 6 points
char *dedup(char *s); // 10 points
char *pad(char *s, int d); // 10 points