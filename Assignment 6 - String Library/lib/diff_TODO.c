/*
 * Returns the number of positions in which s1 and s2 differ, i.e., it returns the number of
 * changes that would need to be made in order to transform s1 into s2, where a change could
 * be a character substitution, an insertion, or a deletion.
*/
#include "../include/personal_string.h"

diff(char *s1, char *s2) {

}